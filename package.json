{
  "devDependencies": {
    "nodemon": "^3.0.2",
    "vite": "^7.0.6"
  },
  "scripts": {
    "dev": "vite --config vite.config.js",
    "watch": "nodemon --watch ${COURSE}/wly --ext wly --exec \"gleam run\"",
    "build": "gleam run -- --which ${COURSE}"
  }
}
